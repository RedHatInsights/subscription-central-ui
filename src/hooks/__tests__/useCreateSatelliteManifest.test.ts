// import { useMutation, useQueryClient } from 'react-query';
// import useCreateSatelliteManifest from '../useCreateSatelliteManifest';

describe('useCreateSatelliteManifests', () => {
  it('adds two numbers', () => {
    expect(2 + 2).toEqual(4);
  });
});
